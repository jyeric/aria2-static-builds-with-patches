name: GitHub Actions Demo
on: [push]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: prepare jobs
        run: sudo apt-get install -y gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64
      - name: clone aria2
        run: sudo git clone https://github.com/aria2/aria2.git
      - name: mingw-x86_64-w64-build-lib
        run: bash ${{ github.workspace }}/build-scripts/mingw-config/aria2-x86_64-w64-mingw-build-libs
      - name: mingw-x86_64-w64-build-aria2
        run: cd aria2 && bash ${{ github.workspace }}/build-scripts/mingw-config/aria2-x86_64-w64-mingw-config 
      - uses: actions/upload-artifact@v3
        with:
          name: build
          path: ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."